package test;

public class PizzaTest {


	@Test
	public void verifierObjet() {
	Object one_obj = new Long(10);
	assertNotNull("assert not null passed", one_obj);
	}

	private void assertNotNull(String string, Object one_obj) {
		// TODO Auto-generated method stub
		
	}

	
	
	
	assert pizza.getNom() != null;
	
	for(Pizza pizza : pizzas) {
		
	}
	
	
	// TestUpdate 
	// Si la donnée de remplacement existe déjà, on refuse 
	
	// TestAdd 
	// Si la donnée entrée existe déjà, on refuse
	
	// TestDelete 
	// Si la donnée supprimée n'existe pas, on refuse 
	
	// TestRead 
	// Si la donnée demandée 
	
for(Pizza pizzo : pizzas)
	if(pizzo.getCode().equals(pizzo.getCode()) 
			
}